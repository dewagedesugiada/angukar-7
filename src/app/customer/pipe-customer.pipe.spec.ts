import { PipeCustomerPipe } from './pipe-customer.pipe';

describe('PipeCustomerPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCustomerPipe();
    expect(pipe).toBeTruthy();
  });
});
